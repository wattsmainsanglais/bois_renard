import { Messages } from "@/global";
import { TranslationStrings } from "../BookingWrapper";




export default function GiteDescription({desc}: {desc: string}){


    return (
        <p>{desc}</p>
    )
}

